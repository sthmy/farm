{
  "name": "ferma-birzha-worker",
  "private": true,
  "scripts": {
    "sync": "cp ../market.js src/market.js",
    "dev": "npm run sync && wrangler dev",
    "deploy": "npm run sync && wrangler deploy"
  },
  "devDependencies": { "wrangler": "^3.90.0" }
}
